from setuptools import setup, find_packages

setup(
    name="your_app_name",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Other dependencies
        "openpyxl==3.0.9",
    ],
)
